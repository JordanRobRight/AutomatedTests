
namespace QA.Automation.APITests
{
    class TestCaseFixure
    {
        /*
    // https://stackoverflow.com/questions/28603507/c-sharp-nunit-testcasesource-passing-parameter
   [TestCaseSource(typeof(DataSource, "PrepareTestCases", new object[] { param1 })]
   [Category("SmokeTests")]
   public void TestRun(string )

   public void TestCases(string url)
   {
       string updatedUrl = FormatUrl(url);
       string result = string.Empty;

       if (!string.IsNullOrEmpty(updatedUrl))
       {
           result = new Common.WebUtils().GetAuthInfo(updatedUrl, this.userName, this.password);
       }
       Assert.IsFalse(string.IsNullOrWhiteSpace(result));

       Console.WriteLine($"Url: {updatedUrl}");
       Console.WriteLine($"Result: {result}");
       Console.WriteLine("\r\n");
   }
   private class DataSource
   {
       public IEnumerable<ResultOfCallMyMethod> PrepareTestCases(parms)
       {
           foreach (string item in entries)
           {
               yield return callmymethho(parm1)
           }
       }
   }
   */

    }
}
